{
	"name": "@sendra/test",
	"version": "1.0.0",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"private": true,
	"dependencies": {
		"@aws-sdk/client-dynamodb": "catalog:",
		"aws-dynamodb-local": "catalog:",
		"pick-port": "catalog:"
	},
	"scripts": {
		"lint": "biome check src",
		"lint:fix": "biome check --write src",
		"lint:fix:unsafe": "biome check --write src --unsafe",
		"typecheck": "tsc --noEmit",
		"build": "tsc",
		"dev": "tsc && tsc -w",
		"clean": "rimraf dist",
		"test": "echo 'No tests'"
	},
	"devDependencies": {
		"sst": "catalog:"
	}
}
